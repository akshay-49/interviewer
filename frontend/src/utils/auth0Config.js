import React from 'react';

// Auth0 configuration - DEPRECATED
// Auth0 has been completely removed in favor of invite-only architecture
// This file is kept for reference but not used anymore

const Auth0ProviderComponent = ({ children }) => {
    return children;
};

export default Auth0ProviderComponent;

